# c-_templates
Each branch in this repo keeps a c++ program template for video-analylitcs. The main branch keeps the default template which only holds the stream handler and the basic model / onnx session + docker configuration to build your application with onnxruntime.
